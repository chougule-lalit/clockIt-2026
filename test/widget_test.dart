// Widget tests will be added in Phase 2+ when the core UI is built.
// The default Flutter counter test is removed since ClockIt uses GoRouter.

import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('ClockIt placeholder widget test', (WidgetTester tester) async {
    // Placeholder — full widget tests come in Phase 2
    expect(true, isTrue);
  });
}
